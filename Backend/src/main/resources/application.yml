spring:
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: none
  datasource:
    # Release DB
    #url: jdbc:postgresql://ictkem.ru:3254/ict?currentSchema=public
    #username: ict
    #password: 123456
    url: jdbc:postgresql://localhost:5432/unesco?currentSchema=public
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
  http:
    multipart:
      max-file-size: 50Mb
      max-request-size: 50Mb